# Generated by `mandate scan`. This is a starting point, not a manifest.
#
# Every REVIEW line is a judgement the tool catalogue could not supply.
# Effects were guessed from the tool name and default to irreversible,
# because under-calling an effect is the more expensive mistake.
#
# Delete each REVIEW comment once you have confirmed the line under it.

version: 1
agent: "iam-key-agent"

# REVIEW: set the workload identity this agent runs as.
# identity: spiffe://example/agents/iam-key-agent

limits:
  # REVIEW: the most one run may spend. Without this, `reach` has no
  # cumulative limit to search for a way to exceed.
  # total: { amount: 0, currency: GBP }
  depth: 8

tools:
  # List IAM users in the account. This tool retrieves a list of IAM users from your AWS account wit
  - name: "list_users"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Get detailed information about a specific IAM user. This tool retrieves comprehensive informatio
  - name: "get_user"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Create a new IAM user. This tool creates a new IAM user in your AWS account. The user will be cr
  - name: "create_user"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: write
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Delete an IAM user. Args: user_name: The name of the IAM user to delete force: If True, removes
  - name: "delete_user"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires_approval: true

  # List IAM roles in the account. Args: path_prefix: Optional path prefix to filter roles max_items
  - name: "list_roles"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Create a new IAM role. Args: role_name: The name of the new IAM role assume_role_policy_document
  - name: "create_role"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: write
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    # REVIEW: assume_role_policy_document looks like a value argument. A ceiling needs scope_key too.
    # value_arg: "assume_role_policy_document"
    # scope_key: "REVIEW"
    # ceiling: { amount: 0, currency: GBP }

  # List IAM policies in the account. Args: scope: Scope of policies to list ("All", "AWS", or "Loca
  - name: "list_policies"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Retrieve the policy document for a managed policy. This tool retrieves the policy document for a
  - name: "get_managed_policy_document"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["version"]

  # Attach a managed policy to an IAM user. Args: user_name: The name of the IAM user policy_arn: Th
  - name: "attach_user_policy"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires_approval: true

  # Detach a managed policy from an IAM user. Args: user_name: The name of the IAM user policy_arn:
  - name: "detach_user_policy"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires_approval: true

  # Create a new access key for an IAM user. Args: user_name: The name of the IAM user Returns: Dict
  - name: "create_access_key"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: write
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Delete an access key for an IAM user. Args: user_name: The name of the IAM user access_key_id: T
  - name: "delete_access_key"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["access_key"]
    requires_approval: true

  # Simulate IAM policy evaluation for a principal. Args: policy_source_arn: ARN of the user or role
  - name: "simulate_principal_policy"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires_approval: true

  # List IAM groups in the account. This tool retrieves a list of IAM groups from your AWS account w
  - name: "list_groups"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Get detailed information about a specific IAM group. This tool retrieves comprehensive informati
  - name: "get_group"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Create a new IAM group. This tool creates a new IAM group in your AWS account. The group will be
  - name: "create_group"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: write
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Delete an IAM group. Args: group_name: The name of the IAM group to delete force: If True, remov
  - name: "delete_group"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires_approval: true

  # Add a user to an IAM group. Args: group_name: The name of the IAM group user_name: The name of t
  - name: "add_user_to_group"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: write
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Remove a user from an IAM group. Args: group_name: The name of the IAM group user_name: The name
  - name: "remove_user_from_group"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires_approval: true

  # Attach a managed policy to an IAM group. Args: group_name: The name of the IAM group policy_arn:
  - name: "attach_group_policy"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires_approval: true

  # Detach a managed policy from an IAM group. Args: group_name: The name of the IAM group policy_ar
  - name: "detach_group_policy"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires_approval: true

  # Create or update an inline policy for an IAM user. This tool creates a new inline policy or upda
  - name: "put_user_policy"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: write
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Retrieve an inline policy for an IAM user. This tool retrieves the policy document for a specifi
  - name: "get_user_policy"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Delete an inline policy from an IAM user. This tool removes an inline policy from the specified
  - name: "delete_user_policy"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires_approval: true

  # Create or update an inline policy for an IAM role. This tool creates a new inline policy or upda
  - name: "put_role_policy"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: write
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Retrieve an inline policy for an IAM role. This tool retrieves the policy document for a specifi
  - name: "get_role_policy"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Delete an inline policy from an IAM role. This tool removes an inline policy from the specified
  - name: "delete_role_policy"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires_approval: true

  # List all inline policies for an IAM user. This tool retrieves the names of all inline policies a
  - name: "list_user_policies"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # List all inline policies for an IAM role. This tool retrieves the names of all inline policies a
  - name: "list_role_policies"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

# REVIEW: no tool was detected as producing a scope, so every
# `requires` above is currently unreachable. Mark whichever tool
# mints each scope with `produces:`, and set `unbounded: true`
# where the agent can obtain them at will. That flag is what
# decides whether a per-scope ceiling means anything.
# Scopes referenced: access_key, version
