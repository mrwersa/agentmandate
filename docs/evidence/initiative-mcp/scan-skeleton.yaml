# Generated by `mandate scan`. This is a starting point, not a manifest.
#
# Every REVIEW line is a judgement the tool catalogue could not supply.
# Effects were guessed from the tool name and default to irreversible,
# because under-calling an effect is the more expensive mistake.
#
# Delete each REVIEW comment once you have confirmed the line under it.

version: 1
agent: "initiative-agent"

# REVIEW: set the workload identity this agent runs as.
# identity: spiffe://example/agents/initiative-agent

limits:
  # REVIEW: the most one run may spend. Without this, `reach` has no
  # cumulative limit to search for a way to exceed.
  # total: { amount: 0, currency: GBP }
  depth: 8

tools:
  # List Projects
  - name: "list_projects_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # List Writable Projects
  - name: "list_writable_projects_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Visible-project counts grouped by initiative. Lightweight endpoint for initiative landing-card b
  - name: "get_project_counts_by_initiative_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Favorite Projects
  - name: "favorite_projects_api_v1_g"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]
    requires_approval: true

  # Project Activity Feed
  - name: "project_activity_feed_api_v1_g"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]
    requires_approval: true

  # Read Project
  - name: "read_project_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Slim, searchable roster of users assignable to this project's tasks. The assignable set is the p
  - name: "search_project_members_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # List Task Statuses
  - name: "list_task_statuses_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # List Tasks
  - name: "list_tasks_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Create Task
  - name: "create_task_api_v1_g"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: write
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Search tasks by title for autocomplete/pickers. Returns lightweight task info (id, title) for ty
  - name: "autocomplete_tasks_api_v1_g"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]
    # REVIEW: limit looks like a value argument. A ceiling needs scope_key too.
    # value_arg: "limit"
    # scope_key: "guild"
    # ceiling: { amount: 0, currency: GBP }
    requires_approval: true

  # Read Task
  - name: "read_task_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Update Task
  - name: "update_task_api_v1_g"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: write
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]
    # REVIEW: property_values looks like a value argument. A ceiling needs scope_key too.
    # value_arg: "property_values"
    # scope_key: "guild"
    # ceiling: { amount: 0, currency: GBP }

  # Move Task
  - name: "move_task_api_v1_g"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: irreversible
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]
    requires_approval: true

  # List Subtasks
  - name: "list_subtasks_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Create Comment
  - name: "create_comment_api_v1_g"
    # REVIEW: effect guessed from the name. read | write | irreversible
    effect: write
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["calendar"]

  # List Initiatives
  - name: "list_initiatives_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Get Initiative
  - name: "get_initiative_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # List all roles for an initiative with their permissions.
  - name: "list_initiative_roles_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Get the current user's permissions for an initiative.
  - name: "get_my_initiative_permissions_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Get all members of an initiative.
  - name: "get_initiative_members_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Slim, searchable, paginated roster of an initiative's members. Same authorization as :func:`get_
  - name: "search_initiative_members_api_v1_g"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller
    requires: ["guild"]

  # Tasks assigned to the current user across every guild they belong to. An optional ``guild_ids``
  - name: "list_my_tasks_api_v1_me_tasks_get"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # Tasks created by the current user across every guild they belong to.
  - name: "list_my_created_tasks_api_v1_me_tasks_created_get"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

  # List projects across all guilds the current user belongs to. Returns a paginated list filtered b
  - name: "list_my_projects_api_v1_me_projects_get"
    effect: read
    # REVIEW: does this spend the caller's authority or a service account?
    principal: caller

# REVIEW: no tool was detected as producing a scope, so every
# `requires` above is currently unreachable. Mark whichever tool
# mints each scope with `produces:`, and set `unbounded: true`
# where the agent can obtain them at will. That flag is what
# decides whether a per-scope ceiling means anything.
# Scopes referenced: calendar, guild
